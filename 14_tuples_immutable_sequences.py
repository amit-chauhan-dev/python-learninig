# tuples (immutable lists)

coordinates = (10, 20)
colors = ("red", "green", "blue")

print(coordinates[0])
# coordinates[0] = 50 <-- this will give ERROR (immutable)
# useful when data should never change
